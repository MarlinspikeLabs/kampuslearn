import LiveAccount from '../_components/student-entry/LiveAccount';

export const metadata = { title: 'Log in — KampusLearn' };

export default function LoginPage() {
  return <LiveAccount key="login" mode="login"/>;
}
