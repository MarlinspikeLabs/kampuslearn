import LiveAccount from '../_components/student-entry/LiveAccount';

export const metadata = { title: 'Create your account — KampusLearn' };

export default function RegisterPage() {
  return <LiveAccount key="signup" mode="signup"/>;
}
